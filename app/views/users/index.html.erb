<% provide(:title, '登録者一覧') %>
 <h1 style="font-size: 32px; font-weight: bold; margin-top: -30px; margin-bottom: 10px;">登録者一覧</h1>
　<%= paginate @users %>
　 <ul class="users"> 
   <%= render @users %>
　 </ul>
　<%= paginate @users %>
